<Window
    x:Class="NetworkTrayAppWpf.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    ui:WindowHelper.UseModernWindowStyle="True"
    ui:ThemeManager.IsThemeAware="True"
    ui:TitleBar.IsBackButtonVisible="False"
    Title="Network Tray Icon Settings"
    Width="320"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="Manual"
    ShowInTaskbar="False"
    Topmost="True"
    Deactivated="Window_Deactivated"
    Closing="Window_Closing">

    <Border x:Name="RootBorder">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="ContentPanel" Margin="16">
            <!-- Header -->
            <TextBlock Text="NetTray Settings" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <!-- Flyout Style -->
            <TextBlock Text="Left-click action" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="FlyoutStyleComboBox" HorizontalAlignment="Stretch" SelectionChanged="FlyoutStyleComboBox_SelectionChanged">
                <ComboBoxItem Tag="Windows10" Content="Windows 10 Network Flyout"/>
                <ComboBoxItem Tag="Windows11" Content="Windows 11 Control Center"/>
                <ComboBoxItem Tag="QuickSettings" Content="Quick Settings"/>
                <ComboBoxItem Tag="AvailableNetworks" Content="Available Networks"/>
                <ComboBoxItem Tag="Settings" Content="Network Settings"/>
            </ComboBox>

            <!-- Adapter Settings Style -->
            <TextBlock Text="Adapter settings window" FontWeight="SemiBold" Margin="0,16,0,4"/>
            <ComboBox x:Name="AdapterSettingsStyleComboBox" HorizontalAlignment="Stretch" SelectionChanged="AdapterSettingsStyleComboBox_SelectionChanged">
                <ComboBoxItem Tag="Explorer" Content="Explorer window"/>
                <ComboBoxItem Tag="ControlPanel" Content="Control Panel"/>
            </ComboBox>

            <!-- Separator -->
            <Separator Margin="0,16"/>

            <!-- Icon Colors Section -->
            <TextBlock Text="Icon Colors" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <!-- Connected Color -->
            <TextBlock Text="Connected" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <StackPanel Orientation="Horizontal">
                <Border x:Name="ConnectedColorPreview" Width="32" Height="32" CornerRadius="4" BorderThickness="1" BorderBrush="Gray" Margin="0,0,8,0"/>
                <TextBox x:Name="ConnectedColorTextBox" Width="100" VerticalContentAlignment="Center" TextChanged="ConnectedColorTextBox_TextChanged"/>
            </StackPanel>

            <!-- No Internet Color -->
            <TextBlock Text="No Internet" FontWeight="SemiBold" Margin="0,12,0,4"/>
            <StackPanel Orientation="Horizontal">
                <Border x:Name="NoInternetColorPreview" Width="32" Height="32" CornerRadius="4" BorderThickness="1" BorderBrush="Gray" Margin="0,0,8,0"/>
                <TextBox x:Name="NoInternetColorTextBox" Width="100" VerticalContentAlignment="Center" TextChanged="NoInternetColorTextBox_TextChanged"/>
            </StackPanel>

            <!-- Disconnected Color -->
            <TextBlock Text="Disconnected" FontWeight="SemiBold" Margin="0,12,0,4"/>
            <StackPanel Orientation="Horizontal">
                <Border x:Name="DisconnectedColorPreview" Width="32" Height="32" CornerRadius="4" BorderThickness="1" BorderBrush="Gray" Margin="0,0,8,0"/>
                <TextBox x:Name="DisconnectedColorTextBox" Width="100" VerticalContentAlignment="Center" TextChanged="DisconnectedColorTextBox_TextChanged"/>
            </StackPanel>

            <!-- Apply to Light Theme -->
            <CheckBox x:Name="ApplyColorsToLightThemeCheckBox" Content="Apply colors to light theme" Margin="0,12,0,0" Checked="ApplyColorsToLightThemeCheckBox_Changed" Unchecked="ApplyColorsToLightThemeCheckBox_Changed"/>

            <!-- Separator -->
            <Separator Margin="0,16"/>

            <!-- Open JSON link -->
            <Button x:Name="OpenJsonButton" Content="Open settings.json" Click="OpenJsonButton_Click" HorizontalAlignment="Left" Padding="8,4"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
